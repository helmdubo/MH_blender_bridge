# Квитанция Source Protocol v4 — S4 Project Resource Index

Статус: **STOP ДО OWNER-РЕШЕНИЯ `OPEN-V4-19`**. Implementation-кандидат
собирается и проходит доступные автоматические гейты, но не является финальной
готовностью S4 к review/merge: активный норматив противоречиво задаёт lifecycle
orphan-rebind warning и rebuild-identical `Diagnostics`.

## База и ветка

- актуальная owner-base: `origin/main` = `388d13c`;
- ветка: `v4/s4-resource-index`;
- `388d13c` смержен в ветку коммитом `94bce28`;
- решения `OPEN-V4-12`–`OPEN-V4-18` перечитаны и реализованы;
- `reference/`, `golden/`, UE Engine и рабочий Blender-код не изменялись;
- `OPEN-V4-1` остаётся отдельным прежним вопросом с действующим
  nested-alias fail-closed правилом.

Незаявленный ранее коммит `de2226c` подтверждён как мой Ledger-WIP,
автоматически закоммиченный/запушенный окружением. Его логическое имя для
ревью: `feat(MH): remove legacy Ledger coupling from source analysis`;
история ветки не переписывалась и force-push не применялся.

## Реализованный WIP-кандидат

1. Добавлен editor-only `FMHProjectResourceIndex` на stock `SQLiteCore`:
   schema `mh.project_index:4`, пять проекционных таблиц, generation,
   транзакционные FullScan/UpsertPaths/GeneratedAssets refresh и
   normalized logical dump.
2. Open fail-closed проверяет integrity, exact `sqlite_master` signature,
   закрытые словари persisted rows и derived-consistency `ResourceKeys`;
   mismatch/аномалия приводит к delete-and-rebuild без migration.
3. Resolver и change detector переведены на indexed lookup. 10k resolve не
   запускает повторный scan; duplicate/invalid/source dependency closure и
   GeneratedAssets precedence применяются до import plan.
4. Dependencies извлекаются из Material/Composite с provenance и закрытыми
   ролями `texture|placement_mesh|placement_composite`; slot-рёбра оставлены
   S5.
5. GeneratedAssets строятся из union Asset Registry query по любому из ровно
   шести `MH.*` tags без загрузки UObject. Валидируются exact-six, carrier,
   deterministic object path, duplicate claims, `source_blocked`, а у binary
   kinds — тождество `AppliedHash == SourceHash`.
6. Добавлен минимальный editor-only `UMHTextureSourceData`; texture import
   сохраняет receipt и публикует шесть tags. Material/Composite получили
   шестой `MH.SourceHash`.
7. Publish Material/Composite после atomic replace выполняет single-shot
   self-publish upsert; успешный import/publish обновляет GeneratedAssets.
   Material receipt откатывается в памяти при package-save failure, новый
   Adopt-carrier удаляется; уже опубликованный source не откатывается.
8. Probable rename использует только биективные `Disappeared × Appeared`
   endpoints одной generation. Move без rename сохраняет identity и даёт
   `NO_CHANGE`; rename даёт unresolved/orphan старого ключа и unique нового.
9. Deprecated `UMHImportLedger`, Ledger composition и commandlet-флаги
   `-ledger`/`-writeledger` удалены без migration/dual-read.
10. Зарегистрированные S4 diagnostics: `MH_W_PROBABLE_RESOURCE_RENAME`,
    `MH_W_ORPHAN_REBOUND_CONTENT_DIVERGED`,
    `MH_E_AMBIGUOUS_GENERATED_ASSET`; golden registry = `45 E / 13 W`.

## Нормативный блокер

`OPEN-V4-19` оформлен в `docs/QUESTIONS.md`.

08 §3 требует одновременно:

- чистую rebuildable-проекцию всех `Diagnostics` из текущих source tree и
  Asset Registry tags;
- warning только после доказанного перехода ранее orphan-ассета к новому
  candidate и persisted derived-row до следующего импорта.

После появления candidate текущая проекция не отличает orphan-rebind от
обычного `stale`; переход выводится только из прошлой generation. Persist
этого факта нарушает delete-SQLite/rebuild equality, а warning на любой
`stale` создаёт false positive. Самостоятельно выбирать session-only warning,
исключение-history или новый authoritative receipt marker запрещено.

Текущий WIP сохраняет доказанный transition в `Diagnostics`, чтобы проверить
остальной lifecycle; именно эта ветвь **не считается принятой** и будет
переписана по owner-решению. До решения обычный stale не трактуется как
orphan-rebind, автоматический execute S4 отсутствует.

## Проверки WIP-кандидата

```text
python -m pytest tests/ -q
111 passed, 5 skipped

python -m compileall addon/mh4blend tests
PASS

python -m pytest tests/test_s4_ledger_purge.py -q
2 passed

git diff --check
PASS (только ожидаемые LF/CRLF notices Git for Windows)

production static-grep: ledger|writeledger
EMPTY

guarded host build:
Build.bat UnrealEditor Win64 Development
  -EnablePlugin=MimirComposite -NoEngineChanges -NoUBA
PASS

BuildPlugin -StrictIncludes (no unity/PCH), stock UE 5.7.4
Editor 45/45; UnrealGame Development 4/4; Shipping 4/4
BUILD SUCCESSFUL

Automation RunTests Mimir.V4.ProjectIndex
10/10 PASS (зафиксировано также как подмножество нижнего полного прогона)

Automation RunTests Mimir
33 success + 3 success-with-warnings; 0 failed; 0 not-run (36/36)
```

Проверки покрывают rebuild dump + resolver outcomes, schema/dictionary и
derived-state corruption, 10k indexed resolve, self-publish single-shot,
move/rename/orphan states, claimed и pre-existing same-hash rename endpoints,
dependency closure, GeneratedAssets precedence, partial/excess tags, binary
hash identity, ordinary stale без orphan warning, доказанный orphan transition,
texture receipt persistence и Material receipt rollback.

## Что остаётся

1. Получить owner-решение `OPEN-V4-19` и привести orphan warning/Diagnostics к
   ратифицированной форме.
2. Перезапустить focused/full Automation и StrictIncludes на финальном delta.
3. Заменить этот STOP финальной READY-квитанцией и только после этого открыть
   один PR S4. Самостоятельный merge запрещён.
