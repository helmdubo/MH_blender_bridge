# Квитанция Source Protocol v5 — V5-S0 Freeze

Статус: **READY FOR EXTERNAL REVIEW / AWAITING OWNER RATIFICATION**. Это
квитанция исполнителя, не owner acceptance, не разрешение на merge и не gate
для V5-S1.

## База и границы

- base: `origin/main` =
  `f7af7c25dafdc3df7c4bea1930223b5761460d76`;
- ветка: `v5/s0-freeze`;
- production-код `addon/`/`ue/`/`tools/`, `reference/` и UE Engine не
  изменялись;
- `golden/` изменён только в явно разрешённом S0 scope:
  `golden/v5/gaz53/`;
- owner merge этого PR является ратификацией freeze; исполнитель PR не мержит.

## Выполнено

1. Создан полный freeze candidate
   `docs/10_source_protocol_v5_plan.md`. Выжившие контракты 08 перенесены
   внутрь; composite §6 заменён на mandatory `"v": 5`, ordered random options,
   parent-local T/R/S, placement profiles, source closure/resolved plan,
   placement Seed, Blender/UE/runtime/cook contract.
2. Создан `docs/11_v5_agent_slices.md` с последовательными V5-S0…S7,
   parked S8, dual-unity gate и общим end-to-end acceptance.
3. 08/09 получили верхние conditional-v5 supersede banners с fate tables;
   исторический body сохранён.
4. `README.md` и `KICKOFF_PROMPT.md` переведены на v5 freeze target. Прежние
   v4 top-level sections сохранены под явными archive/superseded banners.
5. `docs/QUESTIONS.md` переведён на v5 и получил
   `OPEN-V5-1`…`OPEN-V5-7` с временными fail-closed STOP.
6. Добавлены три topology-only GAZ-53 v5 composites, manifest closure и README.
   Random fixture содержит три ordered synthetic options weight 1; body fixture
   фиксирует parent-local `100 + 25 = 125`.
7. Выбран diagnostic naming freeze candidate:
   `MH_E_COMPOSITE_LEGACY_GENERATION`,
   `MH_E_PLACEMENT_GRAMMAR`,
   `MH_E_DUPLICATE_RANDOM_OPTION_INDEX`,
   `MH_E_PARTIAL_PUBLISH`,
   `MH_E_UNREPRESENTABLE_TRANSFORM`; JSON random violations остаются
   `MH_E_COMPOSITE_GRAMMAR`. Реестр production-кода не менялся: регистрация
   выполняется срезом первого call site.

## Нормативные дыры / STOP

- `OPEN-V5-1` — точный bit contract `mh.random_stream:1` и weighted mapping;
- `OPEN-V5-2` — поле binding `.placement` и математика application;
- `OPEN-V5-3` — NodePath/closure hash/ResolvedSignature bytes;
- `OPEN-V5-4` — UE carrier/generated path/applied state placement profile;
- `OPEN-V5-5` — cross-host predicate TRS/shear;
- `OPEN-V5-6` — authoritative GAZ option tokens/Dagor/profile oracle;
- `OPEN-V5-7` — filesystem aliases reader paths (перенос OPEN-V4-1).

Owner может ратифицировать V5-S0 с сохранением STOP, но V5-S1 не начинает
заблокированную часть до соответствующего ответа. Synthetic GAZ option tokens
не являются production content authority.

## Проверки

### Python regression

```text
python -m pytest tests/ -q -rs
128 passed, 6 skipped in 3.62s
```

Все skips — `bpy` suites вне Blender:
`test_composite_blender_bpy.py`, `test_export_fbx_bpy.py`,
`test_export_material_bpy.py`, `test_import_fbx_bpy.py`,
`test_project_textures_bpy.py`, `test_ui_workflows_bpy.py`.

### S0 fixture/document audit

```text
PASS: duplicate-key-aware JSON parse for manifest and all 3 composites.
PASS: 3/3 composite fixtures parse and start with v=5.
PASS: ordered random options = 3, all weights = 1.
PASS: parent-local probe 100 + 25 = 125.
PASS: manifest source closure contains body + all 3 option meshes.
PASS: v5 fate banners/tables present in 08 and 09.
PASS: changed scope excludes addon/, ue/, tools/, reference/.
PASS: checked Markdown links resolve.
PASS: v4 formula and complete surviving §§4, 5, 9 are byte-identical inside 10.
```

### Diff hygiene

`git diff --check` завершён без whitespace errors. Git сообщил только
ожидаемые LF→CRLF warnings рабочего Windows checkout.
Исторический unchanged body `QUESTIONS.md` содержит прежний trailing space;
он не входит в diff и не исправлялся, чтобы не переписывать evidence.

## Не запускалось

- Blender 4.5 LTS bpy field suite;
- guarded UE build;
- `BuildPlugin -StrictIncludes` non-unity/no-PCH;
- force-unity adaptive-off;
- `Automation RunTests Mimir`;
- PIE/packaged/cook.

Причина: V5-S0 меняет Markdown и явно разрешённые JSON fixtures, но не
production/build inputs. Эти gates обязательны в соответствующих следующих
срезах; их отсутствие не является acceptance реализации v5.

## Осталось

- внешний ai audit крупного freeze-среза;
- owner ответы/явное сохранение STOP для `OPEN-V5-1`…`OPEN-V5-7`;
- owner ratification/merge V5-S0;
- только затем новый V5-S1 от свежего `origin/main`.
