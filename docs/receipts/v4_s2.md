# Квитанция Source Protocol v4 — S2 Material format

Статус: **STOP — НУЖНО РЕШЕНИЕ OWNER**. Это не implementation receipt, не
готовность к review и не разрешение на merge.

## База и границы

- base: `origin/main` = `07d0c9e061fde5f84010d74e8e53a98755130160`;
- ветка: `v4/s2-materials`;
- перед стартом полностью перечитаны актуальные 08 и 09, отдельно сверены
  изменённые 08 §§2, 3, 5;
- production-код, `reference/`, `golden/`, UE Engine и host-проект не
  изменялись.

## Выполненная предварительная работа

1. Инвентаризированы Blender material DTO/legacy core, S1 fail-closed
   surfaces, FBX material-set seam, UI и atomic publish helper.
2. Инвентаризированы UE ResourceKey scanner, settings, source coordinator,
   deprecated Ledger, runtime canonical helpers и отсутствующие после S1
   material importer/publisher/applied-state types.
3. Два независимых read-only прохода сверили Blender и UE стороны против 08
   §5/§7 и acceptance S2.
4. Подтверждено, что name-code cleanup, texture ResourceKey scan, parent path
   resolution, atomic tmp/read-back/replace и Adopt UI сами по себе
   реализуемы без нового норматива. Код не начат, потому что payload и applied
   state, которые они должны обслуживать, неоднозначны.

## Блокеры

- `OPEN-V4-6`: пример 08 §5 сериализует top-level bool, тогда как текст 08 и
  09 S2 запрещают static bools; exact allowed grammar не определена.
- `OPEN-V4-7`: не определён subject/алгоритм `AppliedHash`, без которого
  нельзя честно детектировать сохранённую локальную правку managed MI.
- `OPEN-V4-8`: не определены library-mode overrides и Blender mapping для
  `library`/`sides`.

Вопросы оформлены в `docs/QUESTIONS.md` с временными fail-closed правилами.
Согласно инварианту 09 §1 исполнитель не выбрал ни один из возможных
контрактов самостоятельно.

## Проверки

Кодовые проверки не запускались: code diff отсутствует. Перед STOP проверены:

```text
git status --short
PASS: checkout был чист до добавления этой docs-квитанции и QUESTIONS

git rev-parse origin/main
PASS: 07d0c9e061fde5f84010d74e8e53a98755130160
```

## Что остаётся после решения

После нормативного ответа owner продолжить этот же S2 срез: diagnostic
registry/call-site cleanup, Blender codec/export, UE registry/import/publish,
golden vectors, Python/Blender/UE gates. S3 до приёмки и owner merge S2 не
начинается.
