> Status: NORMATIVE · Architecture version: Recipe Model v2.1 · Supersedes: — (created in D0b)

# RECIPE_EXECUTION_STATUS — фактическая точка продолжения

Архитектурный порядок задаёт `KICKOFF_PROMPT.md` (§5) и `docs/16_recipe_model.md`
(§8). Этот документ задаёт **фактическую** точку продолжения программы: какие
срезы реально смержены в `origin/main`, какой срез следующий, и какие срезы
разблокированы для параллельной работы. Перед началом любого среза исполнитель
читает эту таблицу и начинает **только** срез со статусом NEXT/READY.

| Срез | Статус | PR / примечание |
|---|---|---|
| D0a | MERGED | #62 |
| M0 | MERGED | #63 |
| R0a | MERGED | #64 |
| R0b | MERGED | #65 |
| R1 | MERGED | #66 (acceptance по формулировке v2; П6 закрывает R1.1) |
| D0b | MERGED | #67 |
| R1.1 | MERGED | #69 (внешний исполнитель, приёмка близнеца); квитанция `docs/receipts/recipe_r1_1.md` |
| R0c | MERGED | #71 (внешний исполнитель, приёмка близнеца); реализация `7811eab` попала в `main` fast-forward-push'ем до PR, проверена близнецом постфактум (полный suite 176/176 на host близнеца); квитанция `docs/receipts/recipe_r0c.md` |
| S0 | MERGED | #73 (внешний исполнитель, приёмка близнеца; возврат 1 — racy fingerprint); квитанция `docs/receipts/source_s0.md` |
| S1 | CLOSED (без кода) | OPEN-S-1 закрыт owner'ом, вариант c: S0 достаточен; полевой замер холодного скана — по протоколу M0 §6 |
| S2 | MERGED | #77 (внешний исполнитель, приёмка близнеца; возвраты OPEN-S2-1/S2-2 закрыты нормативно); квитанция `docs/receipts/source_s2.md`. Линия S закрыта: S0 #73, S1 CLOSED (вариант c), S2 #77 |
| R2a | MERGED (близнец) | два PR: **R2a-1** фазовое разделение resolver'а — #79 (`docs/receipts/recipe_r2a_phases.md`); **R2a-2** `FMHCompiledRecipe` + реестр + `RecipeShadowParity` как CI-гейт — #81 (`docs/receipts/recipe_r2a_compiled_recipe.md`). OPEN-R2A-1 закрыт owner 2026-09-02 (docs/16 §9). **Полевой тест owner 2026-09-02 на портфолио (main `4782082`) пройден** — `docs/receipts/field_recipe_r2a_20260902.md`. Preview-путь ещё не production → R2b |
| R2b | MERGED (близнец) | три PR: **R2b-1** `MHMaterializeLayout` — #84; **R2b-2** актор на рецептах — #86; **R2b-3** удаления proof-состояния актора и definition-кэша — #90 (owner: удаления одобрены 2026-09-03; квитанция `docs/receipts/recipe_r2b_proof_state_deletions.md` §6). ADR docs/16: `Status: NORMATIVE` (D0a: «PROPOSED → NORMATIVE после R2b») |
| R2c | MERGED | #88 (внешний исполнитель, приёмка близнеца; возвраты OPEN-R2C-1…4 закрыты нормативно в `docs/contracts/recipe_r2c.md`); квитанция `docs/receipts/recipe_r2c.md`; `Proof.BuildPreflightFullClosure` зелёный → гейт R2b-3 открыт |
| R3a | MERGED (внешний исполнитель, приёмка близнеца) | #102 (`2cc04c4`): пять хэшей/ревизий интерфейса меша + `FMHEndpointInterfaceDelta` в реестре endpoint'ов, снимки Ready переживают `InvalidateAll`, пути материалов только в `MaterialBindingHash` (OPEN-R3A-1 → вариант 1); близнец: 197/197 на голове ветки; квитанция `docs/receipts/recipe_r3a.md` |
| R3b | MERGED (внешний исполнитель, приёмка близнеца) | #108 (`c51f0cc`): reconcile по `FMHEndpointInterfaceDelta` вместо полного rebuild (payload/bounds → refresh, descriptor → миграция своего бакета, collision → recreate physics, binding → материалы), child-рецепт без перекомпиляции родителя, счётчики `RecipesRecompiled/ParentRecipesRecompiled/BucketsRefreshed/BucketsMigrated`; DECIDED-R3B-1/2 приняты, OPEN-R3B-1 закрыт (fc08ca1); близнец: 202/202, strict/force-unity; benchmark 100 placements — не записан, за owner; квитанция `docs/receipts/recipe_r3b.md` |
| RS-1 | MERGED (внешний агент, ресёрч; owner залил в `main` коммитом `28e898d`) | `docs/reference_notes/dagor_composite_build_break_20260903.md` — daEditor: «Split composites» снимает один слой, undo хранит записи `(asset, tm, seeds)`, «Export as composit» = файл на диск без изменения сцены; сводка для R4-pre в §6 |
| R4-pre | MERGED (внешний исполнитель, приёмка близнеца) | #93 (`e8256b6`): Break = один слой рецепта в preview-плоскости (без proof/tag-запросов), дети-композиты остаются `AMHCompositeActor` с сидами родителя, plan-view компоненты не транзакционны, `PostEditUndo` восстанавливает из записи; близнец: 194/194 на generic-хосте; owner на портфолио: дубли после Undo не воспроизводятся (2026-09-03); квитанция `docs/receipts/recipe_r4_pre.md`; OPEN-R4P-1 открыт (fail-closed: сиды родителя) |
| R4-pre-2 | MERGED (внешний исполнитель, приёмка близнеца) | #103 (`c754036`): Break переносит appearance-каналы на `AStaticMeshActor` (default + transient custom primitive data), Build предупреждает о непредставимом состоянии через чистый `MHPreflightBuildComposite` и продолжает; близнец: 198/198 на generic-хосте; квитанция `docs/receipts/recipe_r4_pre2.md`; полевое подтверждение — owner на портфолио |
| R4-pre-3 | MERGED (близнец) | контекст вызова резолвера при Break вложенного композита: сохраняемый `FMHCompositeCallContext` на акторе-ребёнке (`StreamNamespace` = NodePath узла родителя, `AppearanceBoundary` = boundary поддерева), необязательный вход resolver `FMHResolveCallContext`, едет во все три плоскости (preview/proof/runtime); закрывает OPEN-R4P-1 по решению owner 2026-09-04 (вариант близнеца) после аудита `docs/reference_notes/dagor_composit_ue5_audit_20260904.md` §7.A; квитанция `docs/receipts/recipe_r4_pre3.md` |
| R4 | MERGED (близнец) | async-загрузка выбранных endpoint'ов (`FStreamableManager`, состояние `Loading`), placeholder `UMHCompositeSettings::PlaceholderMesh` (default `/Engine/BasicShapes/Cube`), интерактивный путь без `FinishCompilation`; первая admission по завершении идёт через reimport-протокол R3b (`bFirstAdmission` → rebuild с placeholder на реальный меш); red `Mimir.V5.Composite.Async.ColdEndpointLoadsWithoutSyncLoad`, `Perf.SelectedMeshWait` → R4-ожидания; полный suite 205/0, strict/force-unity; квитанция `docs/receipts/recipe_r4.md` |
| R5a | MERGED (близнец) | #111 (`c8495ad`): `UMHInstancePoolSubsystem` как самостоятельный сервис: бакет `{ULevel, дескриптор}` на transient `AMHInstancePoolActor`, swap-remove с двумя картами, generation-хэндлы, `ReverseLookup`, owner-операции Hide/Show/Move/Remove, bulk-скоуп; red `Mimir.V5.Composite.Pool.HandleStability/OwnerOperations`; квитанция `docs/receipts/recipe_r5a.md`. Материализация актора/Outliner/Undo ещё не на пуле |
| R5b-0 | MERGED (близнец) | #113 (`dddae77`): reconcile реимпорта на уровне пула: `UMHInstancePoolSubsystem::ReconcileMesh(Mesh, Delta)` (payload/bounds → refresh, descriptor → миграция бакета с сохранением хэндлов и скрытых слотов, collision → physics, binding → overrides), `GetBucketComponents`; red `Mimir.V5.Composite.Pool.ReconcileMesh`; квитанция `docs/receipts/recipe_r5b0.md` |
| R5b-1a | MERGED (близнец) | #115 (`590a5f9`): пул не переиспользует дрейфнувший бакет: `FindOrCreateBucket` сверяет живой компонент с дескриптором и мигрирует через `MigrateBucket`; переносит в пул инвариант `ISM.BucketPolicyRejectsMutatedReuse`; red `Mimir.V5.Composite.Pool.DriftedBucketIsRetired`; квитанция `docs/receipts/recipe_r5b1a.md` |
| R5b-1 | MERGED (близнец) | #116 (`1eda77d`): размещения материализуют static-листья через пул: строки с `FMHInstanceHandle`, компилятор (полный путь `RemoveOwner`+`Add`, reseed/basis/appearance через хэндлы, ISM на акторе нет), актор (`RemoveOwner` при Clear/Destroy/Undo → OPEN-R-1 закрыт, видимость → `SetOwnerEditorVisibility`, `ReconcileEndpoint` принимает мигрированные пулом бакеты), уведомления → `ReconcileMesh` один раз на мир, Outliner через `ReverseLookup`; red `Mimir.V5.Composite.Pool.PlacementMaterializesThroughPool/UndoRestoresPooledPlacement/ReimportMigratesSharedBucketOnce`; квитанция `docs/receipts/recipe_r5b1.md` |
| R5-F | MERGED (близнец) | #119 (`813deaf`): lifecycle-фиксы по аудиту 2026-09-05: rows/leaf-array sync после миграции бакета, Undo во время Edit завершает сессию, `MigrateBucket` сохраняет placement policy, pool-актор виден в Game View; квитанция `docs/receipts/recipe_r5f.md` |
| R5b-2a | MERGED (близнец) | #121 (`c964202`): подсветка только инстансов owner'а на общем ISM (`SetOwnerSelected`, зеркало редакторского выделения через `OnChanged` element-selection-set), bounds размещения (`GetOwnerBounds`, `GetComponentsBoundingBox` актора → `F`); red `Mimir.V5.Composite.Pool.OwnerSelectionHighlightsOnlyOwner/SelectedPlacementHighlightsAndBounds`; квитанция `docs/receipts/recipe_r5b2a.md` |
| R5b-2b | MERGED (близнец) | #122 (`6789814`): selection-адаптер typed elements для `NAME_SMInstance` на element-selection-set редактора: инстанс пула → `ReverseLookup` → owner-композит (pool-актор никогда), лист фиксируется на акторе; чужие ISM — штатная логика; регистрация в модуле на `OnLevelEditorCreated` (без Outliner); Outliner раскрывает задетый лист; red `Mimir.V5.Composite.Selection.PoolInstanceResolvesToOwnerActor`; квитанция `docs/receipts/recipe_r5b2b.md` |
| R5-M | MERGED (близнец) | #124 (`70a89a4`): ISM-usage базовых материалов при Ready-admission меша (`SetMaterialUsage`, async shader cache, пакет dirty) — убирает ленивый `CheckMaterialUsage` в кадре (минутные фризы owner 2026-09-05); red `Mimir.V5.Composite.Reconcile.AdmissionSetsInstancedMaterialUsage`; квитанция `docs/receipts/recipe_r5m.md` |
| R6-D0 | MERGED (близнец) | #125 (`d11406f`): контекст вложенного редактирования: `BeginEditNestedComposite(root, invocation NodePath)` → draft общего определения ребёнка (`GetEditingDraft`), `FMHCompositeEditContext` (edited/source path/invocation/effective parent/root/consumers/scope), Cancel без записи и без rebuild корня; Commit вложенного — отказ до R6-D2; Outliner: «Edit Contents...» и статус «Editing / Context / Saves»; red `Mimir.V5.Composite.EditContext.NestedInvocationOpensDraft`; квитанция `docs/receipts/recipe_r6d0.md` |
| R6-D1a | MERGED (близнец) | #128: инстансы пула не редактируются напрямую: `AMHInstancePoolActor` — `ISMInstanceManagerProvider`/`ISMInstanceManager` (CanEdit/Move/Delete/Duplicate = false, SetTransform отказывает), клик по листу в Composite Outliner выделяет композит, а не SM-instance element (полевой дефект owner 2026-09-06: сдвиг мимо модели, Cancel не возвращал); red `Mimir.V5.Composite.Pool.InstancesRejectDirectEditing`; квитанция `docs/receipts/recipe_r6d1a.md` |
| R6-D1b | MERGED (близнец) | #129: хэндлы узлов вложенного определения под effective-родителем: `AMHCompositeActor::SetEditScope(invocation)` + `SyncEditScopeHandles` (тег `MHScope.Handle:i`), `Tick`: `EditedLocal = HandleWorld × inverse(EditScopeParentLocal × basis)` → draft `Nodes[i]`, перекомпиляция сессии, другие размещения не меняются до публикации; Cancel восстанавливает; `BeginEditNestedComposite` вводит корень в edit-mode под scope; red `c870e13` (`Mimir.V5.Composite.EditContext.NestedHandlesEditDraftUnderParent`); квитанция `docs/receipts/recipe_r6d1b.md` |
| R6-D2 | MERGED (близнец) | #130: Apply Shared Definition: `CommitEditComposite` вложенной сессии → `CommitNestedEditComposite`: draft ребёнка → canonical preflight → граница источника (`ResetTransaction`) → `MHApplyCompositeV5` + `MHPublishCompositeV5` ребёнка (атомарная запись, уведомление → targeted `ReconcileRecipe` всех размещений); ошибка → `RestoreDefinition` (реимпорт из источника или снимок документа), сессия закрыта; UI: Composite Actions / Composite Outliner «Apply Shared Definition» с подтверждением (имя файла, число размещений), «Cancel Edit Contents»; решение (а) — без ревизионного контроля с Blender; red `576ad1f` (`Mimir.V5.Composite.EditContext.ApplySharedDefinition*`); квитанция `docs/receipts/recipe_r6d2.md` |
| R6-U1 | MERGED (близнец) | #131: Save Unique, procedural variant, из сессии Edit Contents: «Make Child Unique in This Definition» (копия редактируемого определения; вызывающее общее определение перенаправляется на копию и публикуется — все его размещения следуют) и «Make Unique for This Placement» (copy-on-write цепочка определений до root размещения; только это размещение переключается на новый root; потоки уровня root сохраняются через `CallContext` = имя исходного root); random внутри копий под новым именем перебрасывается — явное предупреждение (потоки ключуются путём узла, §2.10); цели валидируются до записи | — |
| R6-U2 | MERGED (близнец) | #132: Bake Current Result: `EMHCompositeUniqueVariant::BakeCurrentResult` — копия редактируемого определения = резидентные листья сессии под вызовом (Mesh/Actor, `Origin` под `<invocation>><def>:`) как plain-узлы с transform = мир листа × inverse(мир вызова); без random/групп/профилей, ничего не перебрасывается; appearance под boundary корня сохраняется, boundary внутри определения — явное предупреждение; UI: те же две области «(Bake Current Result)»; red `8996d24` (`Mimir.V5.Composite.EditContext.BakeCurrentResultKeepsResolvedLeaves`); квитанция `docs/receipts/recipe_r6u2.md` |
| R6-UX1 | MERGED (близнец) | #134: полевой отзыв owner 2026-09-06 по Edit Contents: хэндлы узлов — кликабельные спрайты (`UBillboardComponent`), клик по любой строке редактируемого поддерева в Composite Outliner захватывает её хэндл (gizmo уходит в подкомпозит; `FindSessionHandleForNodePath`, и для корневой сессии); рамка (`UBoxComponent`, wireframe) вокруг редактируемого поддерева (`GetEditScopeBounds`/`GetEditScopeFrame`); дерево Outliner сохраняет раскрытие при обновлении и раскрывает/выделяет редактируемый подкомпозит при Edit Contents; строки вне scope приглушены | — |
| R6-UX2a | MERGED (близнец) | #135: Esc — Cancel, Enter — Apply при активной сессии: `MHEditSessionKeyAction`/`MHHandleEditSessionKey`, Slate input pre-processor (только при фокусе в level viewport), `OnKeyDown` Composite Outliner; Apply — через обычное подтверждение перезаписи | — |
| R6-UX2b | MERGED (близнец) | #136: одна кнопка «Save As Unique Copy...» с диалогом (область + Bake) вместо четырёх пунктов меню в Composite Actions и Composite Outliner | — |
| CE-pre | MERGED (близнец) | #138: дыра из аудита (`docs/reference_notes/MH_Composite_Edit_Mode_Spec_19b7515.md` §9): отложенный Apply захватывает `EditSessionEpoch` subsystem'а (растёт на каждом Begin и на закрытии сессии); `MHRunDeferredEditSessionApply(epoch)` — no-op для закрытой/другой сессии; red `76c75d8` (`Mimir.V5.Composite.EditContext.QueuedApplyIgnoresLaterSession`) |
| CE-0 | MERGED (близнец) | #139: контракт `docs/contracts/composite_edit_ce0.md`: решения owner 2026-09-06 (CE вместо Edit Contents; проекция = компоненты одного transient projection-актора; Undo по BPP; панель только `<breadcrumb> \| Exit` + диалог Yes/No/Cancel; добавление узлов empty/composite/mesh/actor в CE-4b; Composite Outliner остаётся) и верифицированный API UE 5.7.4 (публичный `UEdMode`, `ILegacyEdModeViewportInterface`, show flag `EditingLevelInstance` + `PushLevelInstanceEditingStateToProxy`, transient actor как у `ALevelInstanceEditorInstanceActor`, `ResetTransaction` при входе); fixture `MHCompositeEditFixture.h` (root ×2, child ×2 внутри, mesh + group + random{mesh, empty}, чужой ISM) и 4 характеризационных теста `Mimir.V5.Composite.EditMode.Characterization.*` (что переворачивают CE-2/CE-3/CE-4a — в квитанции `docs/receipts/composite_edit_ce0.md`); визуальный spike (save/PIE/dimming) — на CE-2 |
| CE-1 | MERGED (близнец) | #140: единый владелец сессии `UMHCompositeEditSession` (id, epoch, состояние, замороженный контекст, original, draft) и транзакционный draft `UMHCompositeEditDocument` (reflected `FMHCompositeAssetNode` + session-local GUID, `Modify()` → Undo/Redo восстанавливают всё, кэш typed-view по change serial); subsystem — фасад (`GetEditSession`, `GetEditingDraft` читает draft сессии, мост `SyncDraftFromLegacyEdit` до CE-4a); `MHFlatten/UnflattenComposite*` разделены с ассетом; квитанция `docs/receipts/composite_edit_ce1.md` |
| CE-2a | MERGED (близнец) | #141: suppression lease в пуле: `AcquireSuppression/ReleaseSuppression/IsSuppressed`, две оси слота (`bOwnerHidden`, `SuppressionCount`) → эффективный `bHidden` в одном месте (`ApplySlotVisibility`); Hide/Show владельца не снимает lease; мёртвый хэндл/повторный release — no-op; миграция бакета сохраняет lease; квитанция `docs/receipts/composite_edit_ce2a.md` |
| CE-2b | MERGED (близнец) | #142: флаг `UMHCompositeSettings::bCompositeEditModeV2` (off по умолчанию) выбирает backend целиком; `AMHCompositeEditProjectionActor` (transient/editor-only, spawn как у LI editor instance actor) + `UMHCompositeEditProjection`: граф размещения с draft'ом, скомпилированным тем же компилятором через transient-ассет, `MHResolvePreviewGraph` под замороженным контекстом, SMC на mesh-лист (тот же меш и appearance), хэндл на структурный узел, lease на occurrence, компонент ↔ узел draft'а; `SetNodeTransform` обновляет проекцию, соседний вызов не следует draft'у (CE-ADR-4 доказан); Commit/SaveUnique читают draft сессии под флагом; квитанция `docs/receipts/composite_edit_ce2b.md` |
| CE-3a | MERGED (близнец) | #143 (+#144 unity-fix): `UMHCompositeEditorMode : UEdMode` (невидимый в списке режимов, авторегистрация по CDO) следует сессии: активируется подсистемой после открытия сессии+проекции под `bCompositeEditModeV2`, деактивируется в `ResetEditSession`; чужой `Exit` (другой режим, смена уровня) отменяет сессию; locked context — выделять/редактировать можно только projection-актор сессии; панель во viewport как у Level Instance Edit: иконка, `Composite Edit \| root > child[ *]`, **Save** (interactive commit) и **Cancel** (при dirty вопрос Discard/Stay); `FMHCompositeEditCommands`: `CancelEdit` = Escape (сначала SelectNone, как у движка), `SaveEdit` без chord; show flag `EditingLevelInstance` на level-viewport'ах; `PreBeginPIE`/`OnRequestClose` → `RequestCancel`; квитанция `docs/receipts/composite_edit_ce3a.md` |
| CE-3b | MERGED (близнец) | #145: выделение узла через проекцию: `UMHCompositeEditorMode::SelectComponent` (projection-актор эксклюзивно + компонент узла → gizmo на узле), клик во viewport через `ILegacyEdModeViewportInterface::HandleClick` (узел одним кликом; чужие акторы и ISM-бакеты проглатываются, оси gizmo/пустое место — нет); `UMHCompositeEditProjection::FindComponentForOrigin`; Composite Outliner под флагом: строка → узел, `FocusEditScope` хватает первый узел draft'а, подсказка про Save/Cancel во viewport; editing-tint через `UMHCompositeEditProjection::PushEditingTint` (`PushLevelInstanceEditingStateToProxy` на примитивы проекции, идемпотентно по proxy, после Refresh и из ModeTick; проверка — RHI lane); клавиши под режимом: Esc → `RequestCancel` (отложенно, epoch-guard), Enter — ничего; квитанция `docs/receipts/composite_edit_ce3b.md` |
| CE-3c | MERGED (близнец) | #146: корневое определение под `bCompositeEditModeV2`: `BeginEditComposite` открывает ту же сессию/draft/проекцию/режим, что и вложенный путь (occurrence = всё размещение, lease на все строки), размещение не входит в legacy edit-mode; `CommitEditComposite` под сессией публикует draft; вложенная ссылка атомарна (spec A04: геометрия проецируется и → reference-узел, хэндлов ниже `>` нет — чинит и вложенный случай); квитанция `docs/receipts/composite_edit_ce3c.md` |
| CE-3d | MERGED (близнец) | #147: одна writable-сессия: `UMHCompositeEditorMode::RequestSwitch(path)` (чистая — молча, dirty — Save/Discard/Cancel, no-op в ту же область), `BreadcrumbTargets` + crumb-кнопки панели (переход отложен на тик), «Edit Contents...» в Composite Outliner на любой ссылке при открытой сессии и «Edit MH Composite» → корень; вход «рамкой»: `Enter` выделяет projection-актор эксклюзивно; pivot projection-актора = трансформ occurrence; квитанция `docs/receipts/composite_edit_ce3d.md` |
| CE-4a | REVIEW (близнец) | жест gizmo на узле проекции через `ILegacyEdModeViewportInterface` режима: `StartTracking` — одна транзакция, `InputDelta` — мировой трансформ по семантике движка → локаль узла под родителем (`GetParentWorldForComponent`) → `SetNodeTransform`, проекция следует; `EndTracking` закрывает, пустой жест отменяется; рамка (actor-only) жест проглатывает; Undo/Redo внутри сессии восстанавливают draft (`OnRestored` → refresh проекции), сессия жива; граница истории по BPP — `ResetTransaction` в `Enter`/`Exit`, Cancel под CE-backend без транзакции; квитанция `docs/receipts/composite_edit_ce4a.md` |
| CE-4b | NEXT (близнец) | структурные команды draft'а: добавить empty/composite/mesh/actor узел (в т.ч. drag & drop из Content Browser), удалить/дублировать/reparent, Details узла; проекция и Outliner следуют draft'у | — |
| CE-5 | PLANNED (близнец) | publish recovery: NoExternalChange/SourceCommitted/PartialBatch, draft не теряется при recoverable ошибке публикации | — |
| CE-6 | PLANNED (близнец) | lifecycle и cutover: флаг → default on, снятие legacy-пути (хэндлы/`Tick`/`SetPlacementEditMode`), PIE/save map/level change, полевые сценарии owner | — |
| R6-O | PLANNED (опционально, последний в семье R6) | persistent instance overrides (бывший `NodeOverrides`): identity узла = `NodeUID` + occurrence address, состояния Applied/Inactive/Orphan/Conflict, parity Preview/Proof/Export/Runtime, wire-формат v6 с планом миграции | — |
| R7-0 | PLANNED | capability-контракт actor-листьев: поддержанные классы и их preview lifecycle, разрешённые параметры, appearance/seed inputs | — |
| R7-1 | PLANNED | миграция существующего `UChildActorComponent`/`TryLoadClass` пути компилятора на контракт R7-0; `gameobj` placeholder не становится исполняемым actor автоматически, без generic-сериализации всех Blueprint properties, без автопулинга actor-листьев | — |
| R8 | PLANNED | нормализация весов/random math — не первый кандидат; только после замеров (Owner→handles index, Component→bucket index, реальные physics/nav-затраты, аллокации графа/плана в `MHMaterializeLayout`) | — |

Решение owner (а), 2026-09-06 (R6-D2): Blender остаётся единственным
источником истины для `.composite`; двустороннего контроля ревизий нет —
более свежий `.composite`, выгруженный из UE, может быть перезаписан
последующим экспортом из Blender без конфликта. Принятое решение (закрывает
OPEN-R-9, `docs/16_recipe_model.md` §9), не открытый вопрос. Runtime backend
(OPEN-R-8) и World Partition (OPEN-R-5) — отдельные контракты вне R7.

Обновление этой таблицы — обязательная строка acceptance каждого среза.

Правило исполнения (owner 2026-09-02): срезы, которые близнец не делает сам, выполняет внешний исполнитель по контракту из `docs/contracts/`; близнец пишет контракт (ветка + red-тест уже в ветке) и проверяет результат. Каждый срез — отдельная ветка и PR.

Правило DECIDED/STOP (owner 2026-09-05): исполнитель решает на месте и фиксирует `DECIDED-<срез>-N` в контракте, если решение внутри закрытого списка файлов, не трогает тесты/публичный API/resolver/норматив/коды диагностик и при противоречии контракта следует red-тесту; ревьювер принимает или откатывает на ревью. `STOP + OPEN` — только для остального (KICKOFF §7).
